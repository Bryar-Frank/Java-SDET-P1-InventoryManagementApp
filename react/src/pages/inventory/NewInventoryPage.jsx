import NewProductForm from "../../components/forms/NewProductForm"

export function NewInventoryPage() {
    //This is just a placeholder for the page that contains the New Inventory Form
    //Having the page here instead of just the table allows for easier
    //expandability as the app gets more complex

    return <>
        <NewProductForm/>
    </>
}